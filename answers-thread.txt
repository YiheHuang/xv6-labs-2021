当两个线程同时向哈希表插入数据时，总插入速率达到每秒 43927 次，大约是单线程运行 ph 1 时的两倍。这种接近 2 倍的“并行加速”符合预期——两核同时工作应完成两倍任务。

在查看 ph.c 中的插入逻辑（insert 函数）后发现，这是多线程访问哈希表时出错的根本原因。假设一个线程调用 insert 尚未返回，另一线程也调用了 insert，若它们操作的是同一个 bucket（即参数 n 相同），就可能导致部分键值对未能正确插入。

换句话说，这里存在竞争条件：两个线程同时访问并修改共享内存结构，引发插入丢失。
